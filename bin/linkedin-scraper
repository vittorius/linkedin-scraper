#!/usr/bin/env ruby

require_relative '../lib/linkedin-scraper'
if ARGV[0] == '-a'
  profile = Linkedin::AuthenticatedProfile.new(ARGV[1], ARGV[2])
else
  profile = Linkedin::Profile.new(ARGV[0])
end
#puts profile.to_json # FIXME:
puts profile.connections
